export type Credentials = {
    email: string;
    password: string;
}

export type User = {
    birthday: string;
    email: string;
    gender: string;
    id: number;
    name: string;
    state: string;
    avatar: string;
}

export type Token = string;